# Agent-to-Agent Dependencies
boto3>=1.42.0
botocore>=1.42.0
requests>=2.31.0

# Agent framework: owns the LLM agent loop, Bedrock model invocation, and
# credential/region resolution (replaces the hand-rolled discovery + converse).
strands-agents>=1.0.0

# Optional: Slack integration
slack_sdk>=3.21.0

# Optional: FastAPI server
fastapi>=0.104.0
pydantic>=2.0.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Optional: Flask Demo UI
flask>=3.0.0
